@model DusColl.vmHome
<style>
    td:not(:first-child) {
        white-space: nowrap;
    }

    td:last-child {
        width: 2%;
    }

    .btn {
        padding: 6px 7px;
        margin-bottom: 0;
        /*border-radius: 0;*/
        /*border: 0;*/
    }

    .btngridx {
        padding: 1px 5px;
        margin-bottom: 0;
    }

    .btngrid {
        padding: 6.7px 7px;
        margin-bottom: 0;
        border-radius: 0;
        border: 0;
    }
</style>

<table class="table table-striped table-hover table-bordered" id="table_txvry">
    <thead>
        <tr>

            <th>Verifikator</th>
            <th>Jumlah</th>
        </tr>
    </thead>
    <tbody>
        @if (Model.TodoCVERFY != null)
        {
            var i = 0;
            foreach (System.Data.DataRow item in Model.TodoCVERFY.Rows)
            {
        <tr class="DrillDownRow">
            <td width="90%" align="left">@item["NAMAPIC"].ToString()</td>
            <td width="10%" align="center">@item["Veryfy"].ToString()</td>
        </tr>
            }

        }
    </tbody>
</table>